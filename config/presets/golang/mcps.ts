import { createConfigMCPs } from "@/config/helpers";

export default createConfigMCPs({
  // "language-server": {
  //   command: "go",
  //   args: ["run", "github.com/isaacphi/mcp-language-server@latest", "--workspace", ".", "--lsp", "gopls"],
  //   env: {
  //     // LOG_LEVEL: "INFO",
  //   },
  // },
});
